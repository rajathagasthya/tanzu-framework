apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: tanzu-lookups-manager
  name: tanzu-lookups-controller-manager
  namespace: tkg-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tanzu-lookups-manager
  template:
    metadata:
      labels:
        app: tanzu-lookups-manager
    spec:
      containers:
        - image: projects-stg.registry.vmware.com/tkgdev/ragasthya/demo/tanzu-lookup-manager:latest
          imagePullPolicy: IfNotPresent
          name: manager
          resources:
            limits:
              cpu: 100m
              memory: 30Mi
            requests:
              cpu: 100m
              memory: 20Mi
      serviceAccount: tanzu-lookups-manager-sa
      terminationGracePeriodSeconds: 10
      tolerations:
        - effect: NoSchedule
          key: node-role.kubernetes.io/master
